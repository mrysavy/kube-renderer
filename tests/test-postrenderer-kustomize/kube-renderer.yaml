helm_postrenderer:
  test01:
    type: kustomize
    data: |
      apiVersion: kustomize.config.k8s.io/v1beta1
      kind: Kustomization

      namespace: postrendered

      resources:
        - <HELM>
