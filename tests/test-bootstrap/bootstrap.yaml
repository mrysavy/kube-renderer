apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Metadata.name }}
  namespace: {{ if has .Metadata "namespace" }}{{ .Metadata.namespace | default "bootstrap" }}{{ else }}bootstrap{{ end }}
{{ if ne "bootstrap" .Metadata.name -}}
data:
  param1: {{ .Values.param1 }}
  param2: {{ .Values.param2 }}
{{ end }}
